package com.the3Cgrp.zupptrade.shared.enums;

/**
 * Direction of a single-leg futures position.
 * LONG = BUY entry (target above, stop below); SHORT = SELL entry (target below, stop above).
 */
public enum TradeDirection {
    LONG,
    SHORT
}
